% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_safe_brms.R
\name{fit_safe_brms}
\alias{fit_safe_brms}
\title{Fit a Safe BRMS Model}
\usage{
fit_safe_brms(
  formula,
  data,
  family = gaussian(),
  prior = NULL,
  chains = 4,
  iter = 2000,
  warmup = 1000,
  ...
)
}
\arguments{
\item{formula}{A formula describing the model to be fitted in brms syntax.}

\item{data}{A data frame containing the variables in the model.}

\item{family}{A description of the response distribution and link function.
Can be a family function, a call to a family function or a character string
naming the family. Default is gaussian().}

\item{prior}{Prior specifications for model parameters. Default uses brms default priors.}

\item{chains}{Number of Markov chains. Default is 4.}

\item{iter}{Number of total iterations per chain. Default is 2000.}

\item{warmup}{Number of warmup iterations per chain. Default is 1000.}

\item{...}{Additional arguments passed to brms::brm.}
}
\value{
A brmsfit object containing the fitted model.
}
\description{
Fits a Bayesian regression model using brms with safe testing in mind.
This is a wrapper around brms::brm that ensures proper setup for subsequent
e-value calculations.
}
\examples{
\dontrun{
# Simulate data
set.seed(123)
n <- 100
x <- rnorm(n)
y <- 0.5 * x + rnorm(n)
data <- data.frame(y = y, x = x)

# Fit model
fit <- fit_safe_brms(y ~ x, data = data)
}

}
